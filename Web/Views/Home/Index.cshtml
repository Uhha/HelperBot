@{
    ViewData["Title"] = "Home Page";
}

@using DatabaseInteractions;
@using Tracer;
@using System.IO;

    <div class="text-center">

        <h1 class="display-4">From Config</h1>

        <p>LOG:</p>
        @foreach (var item in TraceError.GetLog())
        {<p>@item</p>}

    </div>
